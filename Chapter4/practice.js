//console.log(typeof 42); // practicing using the typeof operator

//console.log('Bruce's beard'); // testing to see if this will give an error

//console.log(Number('3 3')); // seeing if this type conversion function returns NaN (it does)

console.log(Number('three')); // seeing if this type conversion function returns NaN (it does)
